# Only needed in production, where the frontend (Vercel) and backend
# (Railway) are on different origins. Leave unset for local dev - Vite's
# proxy (vite.config.ts) forwards /api to the local backend instead.
VITE_API_BASE_URL=https://your-backend.up.railway.app
